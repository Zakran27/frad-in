export default function Loading() {
  return (
    <section className="min-h-[40vh] flex items-center justify-center py-12">
      <div className="animate-pulse text-gray-500">Loading…</div>
    </section>
  )
}
